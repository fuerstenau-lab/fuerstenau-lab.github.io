<div class="col">
  <a href="{% if team.redirect %}{{ team.redirect }}{% else %}{{ team.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if team.img %}
        {%
          include figure.liquid
          loading="eager"
          path=team.img
          sizes = "250px"
          alt="team thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ team.title }}</h2>
        <p class="card-text">{{ team.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if team.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ team.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if team.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ team.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
